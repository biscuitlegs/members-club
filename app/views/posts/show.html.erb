<h1><%= @post.member.email %></h1>
<p>
    <%= @post.body %>
</p>

<% if member_signed_in? && current_member.id == @post.member.id %>
    <%= link_to "Edit", edit_post_path(@post) %>
    <%= link_to "Delete", @post, method: :delete, data: { confirm: "Are you sure you want to delete this Post?" } %>
<% end %>

<%= link_to "<<Back", :back %>